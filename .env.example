# MongoDB Atlas 免费版连接字符串
# 获取方式：cloud.mongodb.com → 创建免费集群 → Database → Connect → Drivers
# 替换 <username> 和 <password> 为你的数据库用户名和密码
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/product-promo?retryWrites=true&w=majority
